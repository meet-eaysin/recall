{
  "$schema": "https://turborepo.dev/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "globalEnv": ["MONGODB_URI", "JWT_SECRET", "QDRANT_API_KEY", "JEST_WORKER_ID"]
    },
    "lint": {},
    "test": {},
    "test:e2e": {}
  }
}
